\subsection{REQ3: Backend Query Processing Requirements}

\text{It should be able to receive user query from REQ4(the frontend) and call REQ1(the search engine) with the received input.\\}
\text{Then it should receive pages with highlighted paragraphs that feature the information the user asked for from REQ1, and then send that onwards to REQ4 to be displayed to the user.}

